
const numbers = require('./lib');

test('get the number 0', () => {
  expect(numbers.ZERO).toBe(0)
})

test('get the number 1', () => {
  expect(numbers.ONE).toBe(1)
})

test('get the number 2', () => {
  expect(numbers.TWO).toBe(2)
})

test('get the number 3', () => {
  expect(numbers.THREE).toBe(3)
})

test('get the number 4', () => {
  expect(numbers.FOUR).toBe(4)
})

test('get the number 5', () => {
  expect(numbers.FIVE).toBe(5)
})

test('get the number 6', () => {
  expect(numbers.SIX).toBe(6)
})

test('get the number 7', () => {
  expect(numbers.SEVEN).toBe(7)
})

test('get the number 8', () => {
  expect(numbers.EIGHT).toBe(8)
})

test('get the number 9', () => {
  expect(numbers.NINE).toBe(9)
})

test('get the number 10', () => {
  expect(numbers.TEN).toBe(10)
})
